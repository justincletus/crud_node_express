# Node js crud example project with JWT token middleware authentication

The idea of this repository to provide how to build a node js application and setup 

## Available Scripts

In the project directory, you can run:

### `npm install`
### `npm run dev`
or
### `npm start`
